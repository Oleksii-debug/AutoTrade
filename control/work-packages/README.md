# Work package bank

`bank.json` is the imported machine-readable bank of 65 implementation responsibilities.

All imported packages start as architecture state `PLANNED`. No package becomes READY/DONE merely because this file exists.

During bootstrap, repository evidence and dependencies must be materialized before live readiness is computed.
