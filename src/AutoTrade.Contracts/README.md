# AutoTrade.Contracts

C# anchor project for the canonical language-neutral contracts in `/contracts/jsonschema`.

This project deliberately contains no provider-specific economic semantics and no financial authority implementation. It establishes the .NET 10 build boundary and ships the reviewed JSON Schema files alongside the assembly.

Typed bindings will be added only from the canonical schemas and common fixture corpus.
